image: "rust:1.68.2-buster"

# Use cargo to test the project
test-minerva-admin:cargo:
  before_script:
    - cargo install cargo2junit
  script:
    - rustc --version && cargo --version  # Print version info for debugging
    - cargo test --package minerva-admin --verbose -- --format=json  -Z unstable-options --report-time | cargo2junit > results.xml
  artifacts:
    when: always
    reports:
      junit: results.xml

variables:
  POSTGRES_HOST_AUTH_METHOD: trust

integration-test:cargo:
  before_script:
    - cargo install cargo2junit
  services:
    - name: hendrikxitc/minerva
      alias: database
  script:
    - rustc --version && cargo --version  # Print version info for debugging
    - PGHOST=database PGUSER=postgres cargo test --package integration-tests --verbose -- --format=json  -Z unstable-options --report-time --test-threads=1 | cargo2junit > results.xml
  artifacts:
    when: always
    reports:
      junit: results.xml

deploy:
  stage: deploy
  script: echo "Define your deployment script!"
  environment: production


